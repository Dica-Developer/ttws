<watchversion>
  <p>Firmware: { firmware }</p>
  <p>Model: { model }</p>
</div>
  <script>
    this.firmware = 'x-x-x'
    this.model = 'x-x-x'

    updateVersion() {
      this.update({
        firmware: 'y-y-y', 
        model: 'y-y-y' 
      })
    }

  </script>

</watchversion>

